defmodule CffdLiveWeb.DashboardController do
  use CffdLiveWeb, :controller
  alias Phoenix.LiveView

  # def main(%{assigns: %{current_user: current}} = conn, _) do
  #   LiveView.Controller.live_render(
  #     conn,
  #     CffdLiveWeb.DashboardLive.Main,
  #     session: %{cookies: conn.cookies}
  #   )
  # end
end
